{
  "name": "radiopopfm-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:site": "wrangler pages dev apps/site",
    "dev:all": "npm run dev --workspaces --if-present"
  }
}
